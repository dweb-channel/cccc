# CCCC Python SDK

> 预留目录 - Python SDK 将在后续实现

## 计划功能

- 与 TypeScript SDK 对等的 API
- 使用 Python 原生 socket 模块
- Pydantic 数据模型验证
- asyncio 支持

## 参考实现

参见 `src/cccc/daemon/server.py` 中的 `call_daemon()` 函数。
